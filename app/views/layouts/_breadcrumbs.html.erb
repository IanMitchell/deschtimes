<% if @breadcrumbs %>
  <nav class="hidden sm:flex items-center text-sm leading-5 font-medium">
    <% @breadcrumbs.each do |name, url| %>
      <a href="<%= url %>" class="text-gray-100 hover:text-gray-400 transition duration-150 ease-in-out"><%= name %></a>
      <svg class="flex-shrink-0 mx-2 h-5 w-5 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
      </svg>
    <% end %>
  </nav>
<% end %>
